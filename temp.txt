
mov cx,5

mov bp, 0x7000
mov sp,bp
mov bh, 'Y'
push bx


pop bx 

mov ah, 0x0e
mov al, bh
int 0x10

loop:
    mov ah, 0
    int 0x16
    mov [bx], al

    mov ah, 0x0e
    mov al, [bx]
    int 0x10
    inc bx

    dec cx
    cmp cx,0
    je exit

    jmp loop

exit:
    mov ah, 0x0e
    mov al, 124
    int 0x10



jmp $
times 510-($-$$) db 0
db 0x55, 0xaa


pop bx          

    mov ah, 0x0e    
    mov al, byte [bx] ; 
    int 0x10